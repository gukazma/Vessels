# 项目协作约定

## 阶段性提交与推送

用户已授权：完成一个可独立验收的功能、修复或其他阶段性成果后，主动提交并推送到 `https://github.com/gukazma/Vessels.git`。按成果同步，不按固定时间提交，无需再次询问常规提交和推送的许可。

- 当前交付分支为 `master`，远程为 `origin`。推送前核对远程地址和分支；其他任务分支遵循该任务约定，不自动合并到 `master`。
- 提交只包含本阶段已完成的相关修改、素材、测试和文档。不要把其他人的未完成修改混入，也不要产生空提交。
- 修改游戏逻辑或资源后，运行 `tools/check.ps1`；Godot 不在 PATH 时传入 `-Godot 'C:\Program Files\Godot\Godot.exe'`。画面修改还需实际渲染核对。纯文档修改做相应检查即可。
- 保持工程目录整洁，遵守 `.gitignore`，不提交缓存、导出程序、本地凭证和临时截图。
- 使用说明成果的提交信息，正常推送并核对远端提交。不要强制推送或覆盖远端历史；遇到冲突、认证失败或检查失败时保留本地成果并说明原因。
- 完成后简短告知推送结果和提交编号。
